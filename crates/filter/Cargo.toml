[package]
name = "sumidero-filter"
description = "Pure DNS filter engine: ABP-DNS subset + hosts parser and domain matcher"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]

[dev-dependencies]
proptest = "1"
criterion = { version = "0.7", default-features = false }

[[bench]]
name = "lookup"
harness = false

[lints]
workspace = true
